"""Contains the queries used to insert db data,
including the conflict resolution logic and
which fields are actually updateable for each model"""

# Automatically generated by generate_insert_queries.py.
# Do not edit manually.
# Last updated on {{timestamp}}.

# flake8: noqa: E501  # Ignores long lines for better readability

INSERT_QUERIES = {
    "commits": {
        "updateable_model_fields": [
            {% for field in commits["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{commits["query"]}}""",
    },
    "checkouts": {
        "updateable_model_fields": [
            {% for field in checkouts["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{checkouts["query"]}}""",
    },
    "issues": {
        "updateable_model_fields": [
            {% for field in issues["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{issues["query"]}}""",
    },
    "build_definitions": {
        "updateable_model_fields": [
            {% for field in build_definitions["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{build_definitions["query"]}}""",
    },
    "builds": {
        "updateable_model_fields": [
            {% for field in builds["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{builds["query"]}}""",
    },
    "build_runs": {
        "updateable_model_fields": [
            {% for field in build_runs["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{build_runs["query"]}}""",
    },
    "build_run_payloads": {
        "updateable_model_fields": [
            {% for field in build_run_payloads["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{build_run_payloads["query"]}}""",
    },
    "hardwares": {
        "updateable_model_fields": [
            {% for field in hardwares["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{hardwares["query"]}}""",
    },
    "test_definitions": {
        "updateable_model_fields": [
            {% for field in test_definitions["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{test_definitions["query"]}}""",
    },
    "tests": {
        "updateable_model_fields": [
            {% for field in tests["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{tests["query"]}}""",
    },
    "test_runs": {
        "updateable_model_fields": [
            {% for field in test_runs["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{test_runs["query"]}}""",
    },
    "test_run_hardwares": {
        "updateable_model_fields": [
            {% for field in test_run_hardwares["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{test_run_hardwares["query"]}}""",
    },
    "test_run_payloads": {
        "updateable_model_fields": [
            {% for field in test_run_payloads["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{test_run_payloads["query"]}}""",
    },
    "incidents": {
        "updateable_model_fields": [
            {% for field in incidents["updateable_model_fields"] -%}
            "{{ field }}"{% if not loop.last %},
            {% endif %}
            {%- endfor %},
        ],
        "query": """{{incidents["query"]}}""",
    },
}

